# 📄 Simple Static React App

This repository contains a basic static web application built using React.

The project demonstrates fundamental React concepts such as:
- Components
- JSX
- Basic styling

This is a purely front-end project:
- ❌ No APIs are used  
- ❌ No backend integration  
- ❌ No database connection  

---

## 📌 How to Run This Project

Follow these steps carefully:

### Step 1: Download the Project Files

Download all project files from this repository  
OR clone the repository using:

```bash
git clone <your-repository-url>
```
### Step 2: Open Project Root Directory

Open terminal and go to the project root folder (where package.json is located):
```bash
cd <project-folder-name>
```
### Step 3: Install Node Modules

Run the following command to install node modules:
```bash
npm install
```
### Step 4: Create Production Build

To generate a build, run this command from the root directory:
```bash
npm run build
```

This will create a production-ready build folder.

(Optional) Run Development Server

If you want to run the project locally:
```bash
npm start
```

The app will run at:
http://localhost:3000
